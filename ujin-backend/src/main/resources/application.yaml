spring:
  application:
    name: ujin-backend
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:ujin}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
  liquibase:
    change-log: classpath:/db/changelog/master.yaml
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

ujin:
  base-url: https://hck-api.unicorn.icu/
  sync-token: ${UJIN_SYNC_TOKEN:}

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  image-bucket: ${MINIO_IMAGE_BUCKET:images}

outbox:
  scheduler:
    fixed-delay: ${OUTBOX_SCHEDULER_FIXED_DELAY:1000}

news:
  scheduler:
    sync-enabled: ${NEWS_SYNC_ENABLED:true}
    cleanup-enabled: ${NEWS_CLEANUP_ENABLED:true}
    sync-fixed-delay: ${NEWS_SYNC_FIXED_DELAY:300000}
    cleanup-fixed-delay: ${NEWS_CLEANUP_FIXED_DELAY:60000}
